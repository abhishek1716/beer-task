import { AbbreviatedPipe } from './abbreviated.pipe';

describe('AbbreviatedPipe', () => {
  it('create an instance', () => {
    const pipe = new AbbreviatedPipe();
    expect(pipe).toBeTruthy();
  });
});
